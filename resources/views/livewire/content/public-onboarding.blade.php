{{-- Full wizard (business → … → first articles → account). The domain was
     captured on the landing page; mount() redirects here only with a session. --}}
<div>
    @include('livewire.content.partials.wizard')
</div>
